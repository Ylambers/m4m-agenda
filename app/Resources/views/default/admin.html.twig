{% extends 'base.html.twig' %}

{% block body %}
    <div class="container-input">
        <a href="https://www.move4mobile.com/nl/">
            <div class="header-img"></div>
        </a>

        <div class="title">
            <h2> Admin Panel |</h2>
        </div>

        <a href="/loguit">  </a>

    </div>

    <div class="container">
        <h2>Verwijder of pas een afspraak aan</h2>
    </div>

    <div class="container">
        <h2>Maak een nieuwe ruimte aan</h2>
        {{ form_start(formRoom) }}
        {{ form_widget(formRoom) }}
        {{ form_end(formRoom) }}
    </div>

    <div class="container">
        <h2>Maak een nieuwe gebruiker aan</h2>
        {{ form_start(user) }}
        {{ form_widget(user) }}
        {{ form_end(user) }}
    </div>



{% endblock %}